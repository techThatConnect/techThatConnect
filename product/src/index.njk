---
title: Tech That Connect
layout: "base.njk"
---
   <h3 class="subtitle">Helping people become more technically literate about the internet</h3>
{%for post in collections.feature%}

<div class="col">
      <div class="card">
                    <h2 class="card-title">{{post.data.title}}</h2>
            <h4 class="card-subtitle">{{post.data.subtitle}}</h4>
            <small class="card-info">Author: {{post.data.author}}</small> <small class="card-info">last updated: {{post.data.date}}</small>
            <small class="card-info">tags: webdev</small>
                    <p class="card-body">{{post.data.summary}}</p>
            </div>       
        </div>
{%endfor%}